= form_with(model: lady) do |form|
  - if lady.errors.any?
    #error_explanation
      %h2
        = pluralize(lady.errors.count, "error")
        prohibited this lady from being saved:
      %ul
        - lady.errors.each do |error|
          %li= error.full_message
  .actions
    = form.submit
