<%= form_with(model: lady) do |form| %>
  <% if lady.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lady.errors.count, "error") %> prohibited this lady from being saved:</h2>

      <ul>
        <% lady.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
